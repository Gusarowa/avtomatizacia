# Базовый прототип работает БЕЗ внешних зависимостей (только стандартная библиотека Python 3.9+).
# Установка ниже нужна, только если хочешь включить реальный LLM-слой (необязательно):
#
#   pip install -r requirements.txt
#
# Без этого пакета агент автоматически переключается на детерминированный fallback.
openai>=1.0.0
