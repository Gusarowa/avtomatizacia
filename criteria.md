# Критерии поиска

Это входные требования пользователя. Агент читает этот файл (формат `ключ: значение`),
а пустые поля можно не заполнять — для них применяются разумные значения по умолчанию.

- level: junior, intern
- direction: продуктовая аналитика
- city: Москва
- remote: ok            # ok = удалёнка приветствуется, no = только офис, only = только удалёнка
- skills: sql, python, ab-test
- max_age_days: 30       # вакансии старше N дней штрафуются/отсекаются
- min_salary: 50000      # необязательно; ниже — штраф, а не жёсткий отсев

## Свободный комментарий (для LLM)
Ищу первую работу/стажировку именно в продуктовой аналитике (метрики, A/B-тесты,
воронки, дашборды), а не в дата-инженерии или классической backend-разработке.
Готов(а) к удалёнке. Важны менторство и адекватный порог входа без требования большого опыта.
Смущают вакансии, где по факту нужен middle/senior или стек, которого я не знаю.
